# Domain Glossary

Terms are added as they are resolved; `/domain-modeling` owns this file's growth.

## Geergit Root Helper

This repo's application (`geergit_root_helper`): a Flutter client shipped for Android, Linux, macOS, and Windows. Display name `Geergit Root Helper`; application id `com.geerxlabs.geergitroothelper`.

## Geergit

The Geergit product this helper supports — the Android identity-spoofing stack developed in the sibling `geergit` repo. The Root Helper is a separate application; it is not the spoofing module.
